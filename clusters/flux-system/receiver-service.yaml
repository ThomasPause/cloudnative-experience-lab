apiVersion: v1
kind: Service
metadata:
  name: receiver
  namespace: flux-app
spec:
  type: LoadBalancer
  selector:
    app: notification-controller
  ports:
    - name: http
      port: 8092
      protocol: TCP
      targetPort: 9292
